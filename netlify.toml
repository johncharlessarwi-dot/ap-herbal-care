# Multi-page static site — no SPA redirect
# Netlify will serve index.html, about.html, services.html, products.html, contact.html directly

[build]
  publish = "."

# Optional: pretty 404 handling (uncomment if you add a 404.html later)
# [[redirects]]
#   from = "/*"
#   to = "/404.html"
#   status = 404
